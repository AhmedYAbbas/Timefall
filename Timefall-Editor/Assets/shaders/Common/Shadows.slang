import Bindings;

float SunShadow(float3 worldPos, float viewDepth, float3 N, float3 L)
{
	return 0.0f;
}

float SpotShadow(float3 worldPos, int index, float3 N, float3 L)
{
	return 0.0f;
}

float PointShadow(float3 worldPos, int index, float3 N, float3 L)
{
	return 0.0f;
}
